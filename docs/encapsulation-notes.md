# Encapsulation

Encapsulation = Hide internal state + expose controlled behavior

## Key Learnings

- State must be private
- Control modification via setters
- Behavior exposed via public methods
- Private methods restrict internal logic
- Object interaction must respect access control